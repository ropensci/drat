general:
  branches:
    only:
      - gh-pages 

machine:
  services:
    - docker

test:
  post:
    ## Build drat packages
    -  docker run -ti -v $(pwd):/data -w /data rocker/ropensci Rscript build.R

deployment:
  production:
    branch: 
      - gh-pages 
    commands:
      - docker run -ti -v $(pwd):/data -w /data rocker/ropensci Rscript deploy_s3.R
